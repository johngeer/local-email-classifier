// THE shell interface: train() and classify_new() only.
//
// All IO, caching, and the linfa solver live here. Adapters and entry points
// are filled in at checklist §3–§5. Empty for now so the core/shell boundary
// exists from commit one.
